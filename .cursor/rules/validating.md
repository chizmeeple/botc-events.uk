When validating this Jekyll site, run `script/cursor-check` from the repo root
instead of calling bundle exec jekyll build directly.

Instead of running `npm test` run `script/cursor-check` from the repo root.
